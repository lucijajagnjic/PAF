#include <iostream>

void jednadzbaKruznice(float x1, float y1, float x2, float y1)
{
    float d;
    d = sqrt(pow((x-p), 2.0) + pow((y - q), 2.0));
    if (d > r)
    {
        cout << "Tocka nije u kruznici." << endl;
    }
    else if (d <= r)
    {
        cout << "Tocka je u kruznici." << endl;
    }
}
