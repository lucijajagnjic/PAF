import math

class VertikalniHitac:
    def vertilaniHitac(self, v0, h0):
        self.vlista = []
        self.hlista = []
        self.v = v0
        self.h = h0

        for i in range():
            self.h = (2*v0) / (2*9.81)
            self.v = (2*v0) - 2*9.81*h0
            self.hlista.append(self.h0)
            self.vlista.append(self.v0)

        return self.vlista, self.hlista
        print("Objekt uspjesno stvoren. Njegova brzina je " f'{self.v0}' ", a visina je" f'{self.h0}')
